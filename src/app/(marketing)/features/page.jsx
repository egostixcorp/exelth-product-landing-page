import React from "react";
import FeatureSection from "@/components/Layouts/grid-layout";

export const metadata = {
  title: "Features — Exelth",
};

const FeaturesPage = () => {
  return (
    <div className="w-full">
      <FeatureSection />
    </div>
  );
};

export default FeaturesPage;
